# OpenPeriod 项目问题清单与改进方案

整理日期：2026-09-19  
审查基线：`19cd990`  
文档状态：逐项实施中；AUTH-01、AUTH-02、AV-01、AV-02、AV-03、AV-04 均已获用户确认（2026-09-20）；SEC-01 仓库侧加固与部署验收清单已完成，服务器侧 HTTPS 验证待项目所有者执行；SCH-01 已确认（迁移 0009 尚未对生产执行，执行前须审阅）；SCH-02、IMP-01、IMP-02、TIME-01、AI-01、QA-01、GROUP-01、DOC-01、UI-01、UI-02、UI-03、UI-04、UI-05、UI-06 已确认；UI-07 本地实现与验证完成，待用户确认。以上均未部署，生产数据库未改动。UI-07 确认后，本清单内的顺序修改项全部完成。

## 1. 已确认的方向与本次范围

项目所有者希望减少验证码邮件依赖，认证方向确定为：**日常使用邮箱＋密码登录，邮箱验证用于注册、老账号首次设置密码以及忘记密码后的身份恢复。** 有效会话继续保持登录，不要求用户每次打开网站都输入密码或接收验证码。

前端希望调整 UI、动画、字体和整体审美。目前提出三套方向，尚未选定；推荐在现有温暖校园气质上精修。本文最初仅作问题整理；项目所有者现已要求按顺序修改，每完成一项后确认一次。当前按问题编号交付，完成一项后停止，确认后再进入下一项。

审查覆盖认证、课表管理、群组、导入、多校作息、共同空闲计算、后台入口和主要前端组件。证据范围如下：

| 检查 | 结果及边界 |
| --- | --- |
| 当前仓库代码 | 本文明确标为代码问题的发现，依据当前基线的控制流和数据关联判断 |
| 自动化检查 | 本轮审查中 TypeScript 检查通过；14 个测试文件、54 项测试通过 |
| 浏览器观察 | 实看了当前部署的登录页、介绍页及手机布局 |
| 登录后页面 | 浏览器没有可用登录状态，相关结论主要依据代码，未操作真实用户账号复现 |
| 生产环境 | 未执行数据库写操作，也未验证生产数据库具体受影响人数；线上部署与本地提交不作完全一致的假设 |

测试通过不代表下述跨接口、跨学期和多用户问题已被覆盖。本文不是对整个系统不存在其他问题的保证。

## 2. 优先级与问题总览

- **P0：** 账号身份验证风险，应优先修复。
- **P1：** 影响数据隔离、时间准确性、导入正确性或正式环境安全。
- **P2：** 产品完整性、可维护性、交互与视觉质量改进。

| 编号 | 优先级 | 问题 | 证据性质 | 当前状态 |
| --- | --- | --- | --- | --- |
| AUTH-01 | P0 | 已验证邮箱可以仅凭邮箱地址建立新会话 | 代码确认 | 本地修复与验证完成；用户已确认，未部署 |
| AUTH-02 | P1 | 密码设置、找回及老账号过渡机制尚未建立 | 新认证方案必需工作 | 本地实现与验证完成；用户已确认，未部署 |
| SEC-01 | P1 | 本次访问的 IP 入口仍使用 HTTP | 浏览器观察；配置需核实 | 仓库侧完成（2026-09-20）；入口 HTTPS 验证待所有者执行 |
| AV-01 | P1 | 某周“不去”记录影响其他周的共同空闲 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| AV-02 | P1 | 连续空档按节次拼接，可能跨过未经检查的钟点区间 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| AV-03 | P1 | 未录入课表仍按全天空闲计算 | 代码确认；产品规则需调整 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| AV-04 | P1 | 跨校成员的课表完整度统计使用了群组学期 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| SCH-01 | P1 | 自定义作息被多用户共用，且预览阶段就修改正式作息 | 代码确认 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| SCH-02 | P1 | 多校作息已存在，但校历和教学周数仍受统一默认值限制 | 代码确认 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| IMP-01 | P1 | AI 导入忽略页面新选择的学校 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| IMP-02 | P1 | 跨学校导入后，导入目标与当前查看课表可能不一致 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| TIME-01 | P1 | 按天视图显示时间与服务端网格计算时间可能不一致 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| AI-01 | P2 | AI 每日调用限制仅存在进程内存中 | 代码确认 | 本地修复与验证完成（2026-09-20）；用户已确认，未部署 |
| QA-01 | P1 | 关键认证、跨周、多用户、跨学校流程缺少回归覆盖 | 测试覆盖分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| GROUP-01 | P2 | 群组生命周期管理不完整 | 功能缺口 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| DOC-01 | P2 | 项目文档需要与多校、AI 导入及当前认证行为同步 | 文档维护建议 | 本地同步与核对完成（2026-09-20）；用户已确认，未部署 |
| UI-01 | P2 | 文楷覆盖正文、表单和数字，小字号及密集课表不够清晰 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-02 | P2 | 奶油底、点阵、卡片阴影与彩色玻璃热力格叠加，装饰较多 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-03 | P2 | 热力图高空闲比例使用白字，格内状态过度依赖颜色 | 视觉观察、前端代码与对比度计算 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-04 | P2 | 群组、成员、时间筛选与结果的主次层级不够清楚 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-05 | P2 | 共同空闲手机默认周视图；我的课表仍依赖宽网格横向滚动 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-06 | P2 | 控件、字符图标、圆角、字号与状态表现缺少完整统一规范 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；用户已确认，未部署 |
| UI-07 | P2 | 动效分散，加载和筛选更新缺少整体节奏 | 视觉观察与前端代码分析 | 本地实现与验证完成（2026-09-20）；待用户确认，未部署 |

## 3. 认证与安全

### AUTH-01：已验证邮箱绕过后续身份验证

**现象与影响：** `/api/auth/identify` 仅在邮箱从未验证时要求验证码。已经存在 `emailVerifiedAt` 的账号会直接调用 `createSession`，因此知道他人的邮箱地址就可能以其身份建立会话。邮箱曾经被验证，不等于当前请求者就是邮箱所有者。

**代码依据：** [登录识别接口](app/api/auth/identify/route.ts)、[用户识别逻辑](src/server/auth/identity.ts)、[会话管理](src/server/auth/session.ts)。

**修复要求：** 所有新会话必须来自有效的密码校验或明确授权的邮箱验证流程。关闭“输入已验证邮箱即可登录”的路径；邮件服务未配置或发送失败，也不能退化为免验证登录。

**验收标准：** 无会话时，仅提交任意已注册邮箱不能获得登录会话；错误密码不能登录；邮件配置异常不能绕过认证；有效密码登录可正常建立会话。

#### AUTH-01 本次交付记录（2026-09-19）

本项先关闭“仅凭邮箱建立新会话”的入口。完整密码登录属于 AUTH-02，等待本项确认后实施；上面涉及密码的最终验收条件随 AUTH-02 一并验证。本地过渡补丁尚未部署，不会改变当前线上用户的登录流程。

- `identify` 不再创建会话，所有邮箱都必须进入本次验证码验证；未配置邮件服务时，在查找或创建账号前返回 503。
- `issueEmailCode` 增加共用配置检查，重发入口也不能在缺少配置时写入验证码或退化为免验证登录。
- 已验证邮箱可正常重发验证码；有效验证码登录和首次昵称设置流程保留。
- 前端不再把邮箱识别成功当成登录成功；同步登录页、介绍页、设置页和部署说明中的旧认证文案。
- 本次没有更改数据库结构、撤销历史会话、部署或操作真实用户数据。历史会话的处置仍需在后续认证迁移中单独安排。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 修复前回归复现 | 新增的 19 项路由测试中 7 项失败，复现了已验证邮箱直接登录、邮件未配置仍登录以及老用户不能重发的问题 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过 |
| 认证相关测试 | 5 个文件、35 项测试通过；新增路由与配置守卫测试共 23 项 |
| 全部单元测试 | `pnpm test`：16 个文件、77 项测试通过 |
| 生产构建 | `pnpm build` 通过；构建时显式清空数据库和邮件凭据，避免连接真实服务 |
| 本地页面检查 | 在同样未配置数据库和邮件服务的本地生产构建中提交测试邮箱，页面保持在登录表单并展示不可用提示，未跳转为登录成功 |
| 独立复核 | 未发现本项邮箱登录绕过路径残留或由补丁引入的具体回归 |

验证边界：接口测试中的数据库、邮件及会话依赖为模拟实现，未发送真实邮件；没有以真实账号完成生产登录，也没有据此声称验证码并发消费、发送配额或历史会话迁移已经安全。相关后续事项列入 AUTH-02。

### AUTH-02：引入密码登录及老账号过渡

现有用户表、邮件发送能力和会话机制可以复用。整体属于中等规模的局部改造，粗估涉及十几个文件及少量数据库结构变更，约 2–4 人日，包含测试、迁移和上线验证；具体工作量以实现设计及 HTTPS 部署条件为准。

| 场景 | 目标流程 |
| --- | --- |
| 新用户注册 | 验证邮箱 → 设置密码 → 完成资料 → 登录 |
| 日常登录 | 邮箱＋密码 → 校验成功 → 创建会话 |
| 已登录设备 | 有效会话继续使用，不反复发送验证码 |
| 老账号首次设密码 | 重新验证邮箱 → 设置密码 → 建立新会话 |
| 修改密码 | 验证当前密码或进行明确的重新认证 → 修改密码 → 更新会话策略 |
| 忘记密码 | 申请邮箱恢复凭证 → 验证一次性、短时有效凭证 → 设置新密码 → 撤销旧会话 |

实施要求：

- 用户表增加可空的密码哈希字段，为老账号保留过渡状态；现有用户 ID、课表及群组关系保持连续。
- 使用 Argon2id 等专用密码哈希算法，采用成熟实现及合适参数；不保存明文密码，不直接复用验证码或会话所用的普通哈希方法。
- **老账号首次设置密码必须重新验证邮箱，不能只相信旧登录状态。** 旧认证流程可能允许非本人建立会话，不能以此作为首次设密码的充分证明。
- 注册、首次设密码和密码重置凭证按用途隔离，具备过期时间、尝试次数限制及原子的一次性消费机制。
- 登录和恢复请求设置持久化限流；对外错误提示避免不必要地泄露账号是否存在。
- 注册和重置表单支持密码管理器、粘贴和显示／隐藏密码；不把重复收邮件变成日常登录步骤。
- 建议上线切换时撤销旧会话，让用户重新认证。该操作影响真实用户登录状态，属于后续单独制定并审阅的迁移内容，本次没有执行。
- 密码重置后撤销旧会话；新会话创建和旧凭证失效需要保持一致。

AUTH-01 实施期间确认的既存生命周期问题，也在本项处理：验证码读取和消费、失败次数增加、发送配额检查目前不是原子操作；最新验证码消费后，更早的未消费验证码可能重新成为候选；发送时删除过期验证码会削弱依赖验证码记录计数的 24 小时配额。需要明确旧验证码失效规则，并将配额记录与短期验证码清理解耦。

关键验收：老用户设置密码后仍访问原来的课表和群组；旧会话不能直接设置首次密码；重置凭证不能重复使用或跨用途使用；密码错误和暴力尝试受到限制；哈希不出现在响应或日志中。

#### AUTH-02 本次交付记录（2026-09-19）

**结论：本地实现完成，认证边界修复已通过下列验证；尚未部署，等待本项确认。** 用户对 AUTH-01 的「继续」确认后，本轮只推进 AUTH-02，没有开始 SEC-01、课表算法或整体视觉改版。

已实现的行为：

- 登录页默认邮箱＋密码登录；邮件服务或验证码密钥未配置，不阻断已有密码的正常登录。
- 新用户在验证邮箱并设置密码后才创建账号；首次昵称及学校选择继续保留。
- 老账号选择「首次设密码」，重新验证原邮箱；更新原用户行，保留原 ID、昵称、隐私设置、作息、课程与群组关联。
- 设置页可用当前密码修改密码；忘记密码通过邮箱重置。两种操作都撤销旧会话，事务成功后为本次浏览器设置新会话。
- 密码使用 Argon2id 和随机盐，长度 15–128 字符，不裁剪空格；输入支持密码管理器、粘贴、显示／隐藏和重复确认。
- 新验证码独立于历史验证码表，绑定邮箱、用途、challenge ID；到期、错误次数耗尽、重发或成功使用后均不能再次兑换。
- 发送配额、错误尝试和登录限流持久化；同邮箱认证写操作串行化；密码更新、凭证消费、旧会话撤销和新会话插入同事务。插入失败会整体回滚。
- 登录密码校验完成后，事务内再次检查哈希和版本，阻止与密码重置竞争的旧密码创建会话。
- 所有认证 POST 要求 JSON、限制请求体大小并校验来源。处理了 Next 内部主机名与浏览器 Host 不一致的情况；HTTPS 反向代理可通过 `AUTH_ORIGIN` 固定公开地址。
- 邮件投递失败时作废该凭证、保留配额，对外返回与不符合操作条件的邮箱相同的通用结果，避免通过状态码枚举账号；异常日志不写入邮件供应商返回数据或凭证内容。

实现范围与主要文件：

| 范围 | 文件 |
| --- | --- |
| 凭证与校验 | `src/domain/auth.ts`、`src/server/auth/{credentials,password,rate-limit,request,verification,session}.ts` |
| HTTP 接口 | `app/api/auth/{identify,resend,verify,login,password}/route.ts` |
| 登录和账户表单 | `components/{identity-form,password-field,password-settings,settings-panel}.tsx`、`app/login/page.tsx`、`app/welcome/page.tsx`、`app/globals.css` |
| 数据结构与邮件 | `src/server/db/schema.ts`、`drizzle/0007_password_auth.sql`、`drizzle/meta/_journal.json`、`src/server/mail/directmail.ts` |
| 配置、依赖和验证 | `.env.example`、`DEPLOY.md`、`package.json`、`pnpm-lock.yaml`、`src/server/auth/{login-routes,verification-config}.test.ts` |

验证结果（按边界检查 → 回归 → 全量验证执行）：

| 验证 | 结果 |
| --- | --- |
| 类型与补丁检查 | `pnpm exec tsc --noEmit --incremental false`、`git diff --check` 通过 |
| 原绕过与替代输入 | 旧邮箱单字段请求、旧邮箱＋验证码请求、错误密码、错误用途、不同邮箱／challenge ID、旧 Cookie、跨站请求均不能建立有效会话 |
| 真实 SQL 隔离回归 | `pnpm exec vitest run src/server/auth/login-routes.test.ts`：34 项通过；使用内存 PGlite 执行全部 0000–0007 迁移，保留真实哈希、事务、SQL 查询及接口逻辑，模拟邮件与浏览器 Cookie |
| 合法流程与数据连续性 | 注册、邮箱未配置时的密码登录、老账号首次设密码、修改和重置密码通过；旧账号 ID、课程、群组、成员关系保持不变；密码／验证码失效与回滚断言通过 |
| 全部测试 | `pnpm test`：16 个文件、93 项通过；其中验证码配置守卫 5 项，确保缺少配置时无数据库或发信副作用 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后运行 `pnpm build`，通过；未连接真实服务 |
| 页面检查 | 本地生产构建中检查登录、注册、老账号首次设密码、忘记密码直达页、密码显示切换、邮件不可用提示；390px 手机视口无横向溢出。发现并修复了同源请求误拒绝，补充回归测试 |
| 独立只读复核 | 确认邮件发送错误的状态码可枚举账号；父代理修正并增加已知／未知邮箱相同响应测试。复核没有确认其他凭证绕过或数据关联回归 |

**上线前仍需完成的验证与边界：** 没有发送真实邮件，没有对真实账号操作；PGlite 是单连接的 PostgreSQL 引擎，其并发调用测试不能代替多连接／多实例压测。本机未运行独立 PostgreSQL 或 Docker，因此 Linux Alpine 原生 Argon2 模块与真实连接池需在预发布环境验收。页面检查没有在浏览器中完成真实邮件注册或修改真实密码，相关业务流程由隔离接口测试验证。通用响应尚未消除邮件发送路径的耗时差异；没有宣称完全消除了账号存在性的时间侧信道。部署侧全站／IP 限流、异步邮件队列和日志运维仍可后续补强。

**迁移影响需明确审阅：** `0007` 为增量结构变更，不删除用户、群组、成员或历史会话；新应用启动后，旧会话版本为空而不能继续认证。上线时老用户需要重新验证邮箱并首次设置密码，之后日常用密码登录。此切换尚未执行。完整 SQL 和备份、配置、切换顺序见 [迁移文件](drizzle/0007_password_auth.sql) 与 [部署说明](DEPLOY.md)。先完成下一项 SEC-01 的 HTTPS 验收，才能将密码流程开放给真实用户。

### SEC-01：正式入口的 HTTPS 与 Cookie 配置

本轮访问的 IP 入口为 HTTP。代码中存在 `ALLOW_INSECURE_COOKIE=1` 的临时兼容开关，但本次没有读取生产环境变量，不能据此判断开关当前是否开启。

**代码依据：** [会话 Cookie 配置](src/server/auth/session.ts)。

密码登录正式上线前应统一使用 HTTPS，明确 HTTP 到 HTTPS 的入口策略，并关闭生产环境临时允许不安全 Cookie 的配置。验收时检查实际部署的传输协议、Cookie 标记和登录保持行为。

#### SEC-01 本次交付记录（2026-09-20）

本项的实质是部署验收，仓库内可完成的部分已交付；实际入口的协议与 Cookie 行为必须在服务器上按清单检查后本项才算关闭。

- 生产会话 Cookie 现在无条件带 `Secure`；历史 `ALLOW_INSECURE_COOKIE` 开关已失效，任何环境变量都不能再把生产 Cookie 降级。开发环境仍可使用 localhost HTTP。
- 新增全站 [Proxy](proxy.ts) 与共享[传输策略](src/server/security/transport.ts)：生产必须配置唯一 HTTPS `AUTH_ORIGIN` 和明确可信代理；安全 GET/HEAD 才能 308 规范化，HTTP 写请求直接 426，错误 Host 写请求 421，不重放登录密码或业务写入。
- 认证请求、会话 Cookie 写入、会话读取和退出删除共享同一传输断言；即使未来 Proxy matcher 误改，HTTP 也不能建立或使用登录会话。正式 HTTPS 响应增加 HSTS、`nosniff` 与严格来源策略。
- 新增 [transport.test.ts](src/server/security/transport.test.ts) 和更新 [session-cookie-security.test.ts](src/server/auth/session-cookie-security.test.ts)，覆盖错误配置、代理头变体、规范 Host、开放重定向路径、非幂等请求不重定向、所有路径 matcher 与 Cookie 不可降级。
- [DEPLOY.md](DEPLOY.md) 及 `deploy/` 模板明确 Caddy 配置、应用仅绑定回环地址、环境变量与备案后七项线上验收。

验证结果：

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过 |
| SEC-01 聚焦测试 | 传输、Cookie 与认证边界 3 个文件、47 项通过 |
| 全部单元测试 | `pnpm test`：22 个文件、127 项通过（包含当前工作区内其他已完成项目的测试） |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN`、`TRUST_PROXY_HEADERS` 后 `pnpm build` 通过 |
| 本地生产运行 | 配置测试 HTTPS origin 后，实际请求得到：HTTP GET 308、HTTP POST 426、正确代理标记 200 并带 HSTS、错误 Host POST 421 |

边界：项目所有者确认尚未完成备案。本轮没有登录生产服务器或 Vercel 后台，未读取生产环境变量，未改动任何线上配置；域名、证书、端口隔离和真实 Cookie 行为仍待备案后的线上验收。通过后才满足 [DEPLOY.md](DEPLOY.md) 中 0007 认证切换的前置条件。

## 4. 课表、时间计算与导入

### AV-01：跳过课程没有按周次生效

共同空闲读取 `courseExceptions` 时没有限制具体周次，随后将结果转换成仅包含课程时段 ID 的集合。成员在第 2 周标记“不去”，该课程时段在第 3 周计算时也可能被跳过。

**代码依据：** [成员忙碌区间构建与例外查询](src/server/availability/data.ts)。

**建议：** 先将查看日期换算为成员自己的教学周，再按课程时段、用户和对应周次匹配例外记录。

**验收：** 第 2 周跳过、第 3 周正常上课；跨校开学日不同也按各自教学周匹配；个人课表与群组空闲结果一致；批量“不去”只影响选中的周。

#### AV-01 本次交付记录（2026-09-20）

- 共同空闲的例外查询现在带出 `week`，并按「课程时段 ID → 跳过周集合」聚合；[buildMemberRanges](src/server/availability/data.ts) 沿用原有逻辑先把群查看日期换算为成员自己学期的教学周，再按该周匹配跳过记录。第 2 周标记「不去」不再影响第 3 周。
- 周次语义与写入端对齐：跳过记录本就在成员自己学期的教学周里产生（个人课表页），跨校开学日差异由已有的日期对齐承担；个人课表读取端（`getMySchedule`）本就按周过滤，两条路径现在语义一致，不需要数据迁移。
- 测试：
  - [ranges.test.ts](src/server/availability/ranges.test.ts)：跳过用例改为按周断言（第 2 周无课、第 3 周照常、超出上课周次无课），并新增跨校开学日差异下按成员教学周匹配的用例。
  - 新增 [group-availability.test.ts](src/server/availability/group-availability.test.ts)：PGlite 执行全部迁移，走真实 SQL 的群组共同空闲路径（北大群 + 9/14 开学的对外经贸成员，成员在其第 1 周跳过），断言群第 2 周空闲、第 3 周忙碌、详情接口一致。
- 回归复现：用修复前的 `data.ts` 运行新集成测试，「第 3 周照常忙碌」用例如预期失败，确认测试能抓住本缺陷；恢复修复后全部通过。

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过 |
| 修复前回归复现 | 新集成测试在旧代码下 1 项失败（第 3 周被其他周的跳过影响） |
| 全部单元测试 | `pnpm test`：18 个文件、99 项通过（新增 3 项） |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |

边界：批量「不去」的写入端（`replaceMeetingSkips`）本就按周存多条记录、且校验周次必须在课程范围内，本次未改动其行为，也未为其新增写入侧测试（读取端已由集成测试覆盖）；「个人课表与群组空闲一致」目前由相同周次语义保证，尚未做同数据的双路径对比断言，可随 QA-01 一并补充。

### AV-02：连续空档缺少完整钟点轴校验

当前先按北大节次网格判断空闲，再将相邻空闲格子拼成一段。网格未覆盖的午间、课间等区间不一定经过检查，却可能落在拼接后的推荐起止时间之内。例如成员有自定义的 12:10–12:50 忙碌，而两侧网格均为空闲时，推荐结果可能跨过这段忙碌。

**代码依据：** [显示网格与相交判断](src/server/availability/data.ts)、[连续空档合并](src/domain/free-runs.ts)。

**建议：** 先按真实日期和钟点区间计算所选成员的忙碌并集及共同空闲区间，再生成显示网格。最短时长筛选、列表、详情和复制文本全部基于该结果。

**验收：** 午间和课间忙碌不会被推荐跨越；相邻节次之间有忙碌时不能合并；不同学校节次边界不造成遗漏；晚间超出基础网格的时间也有明确覆盖规则。

#### AV-02 本次交付记录（2026-09-20）

- 共同空档改为按真实钟点轴计算：服务端 [getGroupAvailability](src/server/availability/data.ts) 先把所选成员的全部忙碌区间取并集，再取其在显示网格范围内的补集，新增返回值 `freeIntervals`（按星期给出一组分钟区间）。网格行之间的午间、课间同样经过检查，不会再被相邻空闲格子拼接跨越。
- 显示网格（热力格）与格子级 `slots` 保持原样，仅空档推荐改用钟点轴结果；覆盖范围规则明确为「空档只落在显示网格的最早开始到最晚结束之间」，与原有展示范围一致。
- 新增纯函数 [free-intervals.ts](src/domain/free-intervals.ts)（忙碌并集 + 补集，处理重叠、裁剪与无效轴）；[free-runs.ts](src/domain/free-runs.ts) 的 `FreeRun` 从节次对改为分钟区间，新增 `formatClock`，删除按节次拼接的旧实现。
- 前端 [common-availability.tsx](components/common-availability.tsx)：空档列表、最短时长/白天晚间筛选、时长与复制文本全部改用区间自身的分钟数；列表时间直接由分钟格式化，两端恰好对齐网格行时保留「第 X–Y 节」标注，被忙碌切出的空档标注「按实际钟点」。「已过去时段」排除逻辑保持原语义（今日且区间整体已结束的不再推荐，进行中的区间完整保留）。
- 测试：新增 [free-intervals.test.ts](src/domain/free-intervals.test.ts) 5 项（午间忙碌不跨越、节间忙碌不合并、多成员并集、轴外裁剪、无效轴）；[free-runs.test.ts](src/domain/free-runs.test.ts) 按新签名重写并通过（含进行中区间完整保留）；集成测试新增自定义作息成员（12:10–12:50 私人忙碌，落在北大网格行之间的午间），断言真实 SQL 路径下 `freeIntervals` 把午间切开、忙碌所在周之外恢复完整空闲。

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过 |
| 全部单元测试 | `pnpm test`：19 个文件、107 项通过（新增 8 项，重写 3 项） |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |

边界：时间轴覆盖范围沿用显示网格（基础 12 节 + 实际有忙碌的追加钟点行），不在该范围内的晚间时间仍不参与推荐，这与改版前的展示范围一致；格子级 `slots` 与空档列表现在来自两套计算，结果在语义上一致（同为全员空闲），逐格一致性断言未建立，可随 TIME-01 统一时间来源时一并处理；本轮未在浏览器中做真实登录后的页面级验收。

### AV-03：未知课表状态被计算为空闲

未录入课表的成员当前按全天空闲处理。已有提醒文案，但绿色可约结果仍可能传递过强的确定性。课程数为零还可能表示用户确实无课，不能只靠数量判断完成状态。

**代码依据：** [缺少学期时的默认区间](src/server/availability/data.ts)、[完整度提醒和成员选择](components/common-availability.tsx)。

**建议：** 显式区分“尚未录入／尚未确认”“已录入”“已确认本学期无课”。存在未知成员时，结果显示“已知成员可用，另有 N 人待确认”，不要直接宣称所有人都有空。

**验收：** 未录入、主动确认无课、存在私人忙碌三种情况可区分；人数、颜色、说明和复制文本表达一致。

#### AV-03 本次交付记录（2026-09-20）

**状态模型**（`src/server/availability/data.ts`）：每位成员在某学期处于三态之一——

| 状态 | 判定 | 计算处理 |
| --- | --- | --- |
| 已录入 | 有课程或有私人忙碌记录 | 忙碌照常参与计算 |
| 已确认无课 | 课程与忙碌均为空，且在 `semester_confirmations` 有显式确认 | 按有空参与 |
| 未知 | 三者皆无（含没有学期行） | 不计入「大家都有空」，标为「待确认」 |

关键规则：未知成员既不算空闲也不算忙碌——`commonFree` 仅当不存在未知成员时才成立；`freeCount` 只统计已录入成员；空档（`freeIntervals`）按已录入成员的忙碌并集计算，全部所选成员均未知时为空。仅有忙碌记录、没有课程的成员（「点击空格子标记忙碌」功能的用户）属于已录入，其忙碌照常生效。

主要改动：

- 新增表 `semester_confirmations`（用户×学期的显式无课确认，纯增量）：[drizzle/0008_semester_confirmations.sql](drizzle/0008_semester_confirmations.sql)、[schema.ts](src/server/db/schema.ts)。
- 新增接口 `PUT /api/schedule/confirm-empty` 与服务端 `setSemesterConfirmedEmpty`；`getMySchedule` 返回 `confirmedEmpty`。
- 我的课表页空状态提供「确认无课 / 取消确认」入口，并说明对共同空闲的影响。
- 共同空闲页：格子悬停文案、完整度提醒、详情弹窗（新增「待确认」状态样式）、空档列表头部提示与复制文本，全部改为区分三态的表达；成员徽标悬停说明同步更新（徽标本身的「未录」判定依赖群学期课程数，其跨校准确性属于 AV-04，本次未动）。

验证结果：

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：19 个文件、110 项通过（新增 4 项集成测试：未知成员不参与共有空闲、已确认无课按有空参与、仅有忙碌记录属于已录入、既有 AV-01/02 场景回归） |
| 迁移可执行性 | 集成测试经 PGlite 执行全部 `0000`–`0008` 迁移 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |

边界：群组成员列表的「未录」徽标与课程数统计仍使用群学期口径（AV-04 范围）；旧浏览器标签页的短期缓存响应不含新字段，前端按 undefined 处理为「无未知成员」，与旧行为一致，页面刷新后即恢复准确；本轮未在浏览器中做真实登录后的页面级验收。

### AV-04：跨校完整度统计关联错误学期

成员课程数量按 `group.semesterId` 查询，而成员课程可能归属于其自己学校的学期。因此其他学校成员已有课表，界面仍可能显示“未录入”。

**代码依据：** [群组成员课程计数](src/server/groups/data.ts)。

**建议：** 与共同空闲计算共享成员学期定位规则，并结合显式完整度状态展示。

**验收：** 两名不同学校、均已录入课程的成员，在同一群组中都显示正确状态。

#### AV-04 本次交付记录（2026-09-20）

- 成员学期定位规则抽取为共享函数 [memberSchoolKey](src/server/semesters/data.ts)（跟随成员自己学校的作息预设，custom 作息独立成键），共同空闲计算（`loadAuthorizedGroupSchedule`）与群组完整度统计（`listGroupsForUser`）现在共用同一条规则，不再各写一份。
- 新增共享汇总 [summarizeMemberSchedules](src/server/semesters/data.ts)：按成员各自学校的学期行统计课程数、忙碌数与无课确认，并按与 AV-03 相同的规则给出三态。`listGroupsForUser` 的成员数据由此带出 `courseCount` 与 `scheduleState`，替换原来按 `group.semesterId` 的单表计数。
- 前端成员徽标接入显式状态：「未录」（未知）、「无课」（已确认无课）、「已录入」不标注；悬停说明同步。完整度提醒（`unrecordedCount`）改用三态判定——跨校已录入成员不再因群学期口径误报「尚未录入课表」。
- 群组列表中仅有忙碌记录的成员徽标不再显示「未录」（与共同空闲的「已录入」判定一致）。

验证结果：

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 修复前回归复现 | 旧版 `groups/data.ts` 下新增集成测试精确失败（外贸成员课程数被记为 0）；恢复修复后通过 |
| 全部单元测试 | `pnpm test`：19 个文件、111 项通过（新增 1 项集成测试，覆盖跨校两成员 + 未知/确认无课/仅忙碌三态的群组口径） |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |

边界：admin 页的账号课程总数仍是跨学期全量口径（管理视图语义，与群组完整度无关）；学期行不存在时课程数按 0、状态按未知处理（与共同空闲一致）；本轮未在浏览器中做真实登录后的页面级验收。至此 AV-01～04 的共同空闲准确性问题全部处理完毕，AV-03 交付记录中「徽标属于 AV-04」的遗留边界随之关闭。

### SCH-01：自定义作息缺少用户隔离，预览产生正式副作用

`ensureCustomSemester` 使用统一的 `custom` 学期键，没有用户归属条件，因此多名用户会更新同一份作息。上传预览接口在文件检测及确认前调用该更新，解析失败或取消导入也可能已经改变正式作息。

**代码依据：** [自定义学期更新](src/server/semesters/data.ts)、[上传预览接口](app/api/import/preview/route.ts)。

**建议：** 自定义作息归属于用户或独立、不可变的作息版本。预览阶段仅保存本次草稿和快照，确认时在事务中绑定目标课表。已有共享数据需要制定迁移方案，不能凭当前记录推断每位用户原本的作息。

**验收：** 用户 A 修改作息不影响 B；解析失败或放弃预览不改变正式课表；确认导入后采用预览中核对的版本；历史课表不会因其他人的调整改变含义。

#### SCH-01 本次交付记录（2026-09-20）

**数据模型与迁移**

- 新表 `custom_schedules`：自定义作息按（用户，学年，学期）隔离存储，见 [drizzle/0009_custom_schedules.sql](drizzle/0009_custom_schedules.sql)。
- **存量迁移方案**：0009 建表后，把旧共享学期行（`school = 'custom'`）上的作息原样快照给每位 `schedule_id = 'custom'` 的用户——保持每个人当前实际看到的有效状态，不推断谁原本写了什么（符合本节「不能凭当前记录推断」的约束）；快照与原列都不删除。生产执行仍须项目所有者审阅 SQL 后在维护窗口进行。
- `school = 'custom'` 共享学期行降级为课程数据挂载容器；旧列 `semesters.custom_schedule` 不再写入，仅作为无主自定义群组的只读兜底。

**写入路径**

- 删除了写共享列的 `ensureCustomSemester`，新 `saveUserCustomSchedule` 按用户 upsert（[semesters/data.ts](src/server/semesters/data.ts)）。
- 上传预览接口（[app/api/import/preview/route.ts](app/api/import/preview/route.ts)）不再有任何正式作息写入：自定义行只随预览载荷暂存（`payload.customRows`）。
- 确认导入在原事务内把预览中核对的网格落到用户自己名下（[imports/data.ts](src/server/imports/data.ts)）；解析失败、取消或放弃预览均无正式副作用。

**读取路径（均改为用户名下网格）**

- 我的课表（`getMySchedule`）读本人网格；
- 共同空闲的成员作息读成员本人的网格（节次换算按各自学校/自定义网格，跨校规则不变）；
- 自定义群组（群学期为 custom）的网格跟随**群主**的个人网格，群组列表展示同规则。

**验收对照**

| 验收点 | 结果 |
| --- | --- |
| 用户 A 修改作息不影响 B | 集成测试通过（A 更新后 B 仍为原网格） |
| 解析失败或放弃预览不改变正式课表 | 集成测试通过（预览创建后、第二个未确认预览后，B 的网格均未变） |
| 确认导入采用预览中核对的版本 | 集成测试通过（确认后 B 网格更新为预览中的版本） |
| 存量数据迁移方案 | 0009 回填已在 PGlite 按真实迁移顺序验证（0000–0008 → 造旧共享数据 → 0009 → 断言每位用户获得快照、原列不动） |

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：20 个文件、114 项通过（新增 3 项） |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |

**边界**

- 自定义群组的网格动态跟随群主当前作息（与「群组挂到群主学校学期」的既有语义一致），不是建群时刻的不可变快照；群主改作息会影响其自定义群组的节次换算——影响面相比旧行为（全体共享一行）已收窄到群主本人，完全不可变的作息版本属于 SCH-02 的校历建模范围。
- 0009 未对生产执行；执行前须审阅回填 SQL 与影响范围（见 DEPLOY.md）。
- 本轮未做浏览器级验收。

### SCH-02：多校校历及教学周限制

学校预设主要定义节次和钟点。新学期默认沿用相同开学日、16 周配置，多处查询和写入校验也限制到 16 周；设置页展示的学期信息来自默认配置。不同学校、校区、学期长度及冬夏作息仍需要完整建模。

**代码依据：** [默认学期](src/config/semester.ts)、[学期初始化](src/server/semesters/data.ts)、[课表校验](src/server/schedule/validation.ts)、[空闲查询校验](src/server/availability/query.ts)、[设置页](app/settings/page.tsx)。

**建议：** 将学校、作息版本和学期校历分开管理；教学周数由目标学期驱动。学校预设记录适用校区、季节、来源和更新时间；导入时允许核对开学日期及周数。

**验收：** 不同开学日按同一实际日期正确对齐；合法的第 17–20 周课程可录入和查询；当前学期信息来自实际配置；冬夏作息调整不默默改变历史数据。

#### SCH-02 本次交付记录（2026-09-20）

本轮按「核心校历参数化」范围交付；学校/作息/校历的完整分离建模（预设版本化、导入核对页）列为后续边界。

**教学周数由学期驱动（对应验收：第 17–20 周可录入和查询）**

- 静态校验只保留硬上限 52（与数据库 `semesters_week_count_check` 一致）：课程/忙碌周次、单周与批量「不去」、课表查询周、共同空闲查询周（[validation.ts](src/server/schedule/validation.ts)、[query.ts](src/server/availability/query.ts)、[import-validation.ts](src/domain/import-validation.ts)）。
- 实际周数校验移到数据层、按目标学期的 `weekCount` 执行：课程与忙碌录入（`createCourse`/`updateCourse`/`createBusyBlock`/`updateBusyBlock`，学期由成员学期定位规则决定）、个人课表查询（`getMySchedule`）、确认导入（`confirmImport`）、群组共同空闲查看周（`loadAuthorizedGroupSchedule`）。超周请求返回 400，错误信息包含该学期的实际周数范围。
- 复查发现个人课表编辑与导入链路仍隐含 16 周默认值；现已把实际 `semester.weekCount` 贯穿课程、私人忙碌、批量「不去」、逐周选择器、Excel/AI 解析及导入预览。历史预览没有 `weekCount` 时兼容回落为 16 周。

**各校校历可配置（对应验收：不同开学日按同一实际日期正确对齐）**

- 维护脚本 [apply-semester-config.mjs](scripts/apply-semester-config.mjs) 增加可选 `school` 参数（默认 PKU；其他学校用作息预设 id），每所学校的开学日/周数/时区可各自显式设置；配合已有的按成员学期对齐逻辑，同一实际日期在不同学校正确换算为各自的教学周（对齐逻辑已在 AV-01 测试覆盖，跨校 fixture 使用了 9/7 与 9/14 两个开学日）。
- 首建学期行仍使用全局默认配置——修改校历只走显式脚本，避免任何「默默改变」。

**当前学期信息来自实际配置（对应验收）**

- [设置页](app/settings/page.tsx) 改为按登录用户读取其实际学期行（学年/学期/周数/开学日），不再展示全局默认常量；未登录回落默认配置。

**验证结果**

| 验证 | 结果 |
| --- | --- |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：23 个文件、131 项通过；除服务端第 17–20 周录入、查询和越界校验外，新增覆盖 20 周规则解析、Excel 缺省/显式周次、AI 第 17–20 周及导入预览实际周数 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过（/settings 转为动态渲染） |

**边界（含冬夏作息验收的说明）**

- 冬夏作息：预设已按季节使用不同 id（如 `xjtu-winter`/`xjtu-summer`），学期行固定引用预设 id，切换季节需显式换 id，不会静默发生；但预设行内的钟点仍是代码常量，修改预设内容会改变引用它的历史学期渲染。作息网格的版本化快照未在本轮实施，属完整分离建模（预设记录适用校区/季节/来源/更新时间、导入核对开学日与周数）的后续工作。
- 学期行缺失/未配置的学校仍回落全局默认开学日与 16 周（显式脚本配置后即生效）。

### IMP-01：AI 导入没有采用页面选择的学校

前端 AI 请求仅提交 `mode` 和图片／文字，未提交页面中的 `scheduleId`；后端使用账号中保存的学校。页面文案却表示会按上方所选学校换算，结果可能与用户预期不同。自定义作息也需要在该路径中得到明确支持。

**代码依据：** [AI 导入请求](components/import-upload.tsx)、[AI 导入接口](app/api/import/ai/route.ts)。

**建议：** 所有导入来源使用同一份明确的学校、学期和作息上下文，服务端验证后写入预览；账号默认学校仅作为初始值。

**验收：** 账号学校与页面选择不同时，AI 按页面确认的作息解析；图片、文字、Excel 路径结果一致；自定义作息不会被误认为北大默认值。

#### IMP-01 本次交付记录（2026-09-20）

**结论：本地修复完成，等待本项确认；尚未部署。**

- AI 图片和文字导入现在都显式提交页面当前选择的 `scheduleId`；账号默认学校只负责初始化页面选择，不再被服务端用于覆盖本次 AI 导入上下文。
- 选择自定义作息时，客户端会在请求前检查是否已填写作息行；服务端再次校验行结构、节数、时间格式、先后顺序与重叠情况，并将自定义作息快照写入导入预览载荷。
- AI 接口将学校作息设为必填参数，缺失或未知 id 会返回 400，不再静默回落到北京大学。学期周数按本次选择的学校读取，解析器和预览使用同一个作息快照。
- Excel 导入原本已显式提交并校验页面所选作息，本项没有改变其流程；新增回归测试确认 AI 文字和图片现已遵循相同的“页面选择优先”规则。

验证结果：

| 验证 | 结果 |
| --- | --- |
| AI 导入接口回归 | 新增 7 项：覆盖文字与图片采用页面所选学校、账号默认学校不覆盖、自定义作息进入预览，以及缺失／未知／畸形作息被拒绝 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：24 个文件、138 项通过 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |
| 格式检查 | 本项变更 `git diff --check` 通过 |

边界：本项没有修改账号默认学校、导入确认后的当前课表定位（IMP-02）、AI 次数限制（AI-01）或生产数据；没有执行部署或数据库迁移。

### IMP-02：导入成功后，当前课表可能仍指向旧学校

确认导入将课程写入预览对应的学期，但仅在用户学校为空时更新 `users.scheduleId`。已有学校的账号改选另一学校导入后，个人课表仍按旧学校读取，可能表现为导入成功却看不到新课程。

**代码依据：** [导入确认事务](src/server/imports/data.ts)、[个人课表学期定位](src/server/schedule/data.ts)。

**建议：** 明确区分“账号默认学校”和“当前查看课表”。导入前展示目标学校、学期、作息及替换范围；成功后进入该目标课表。切换默认学校时明确说明对查看结果的影响。

**验收：** 导入成功后立即能查看对应课程；重新登录后目标仍可访问；旧学校数据不会被无提示覆盖；恢复点与被替换的目标学期对应。

#### IMP-02 本次交付记录（2026-09-20）

**结论：本地修复完成，等待本项确认；尚未部署。**

- 创建预览时由服务端写入目标学年、学期、开始日期和实际周数；检查页现在明确展示目标学校（含冬夏作息等变体）、学期、作息节数及替换范围。
- 确认导入会在同一个数据库事务内只替换预览绑定的目标学期课程，并把账号当前课表切换到该目标。切换结果持久化，因此导入完成跳转和重新登录后都能直接看到新课程。
- 北京大学的历史学期行以空 `schedule_id` 表示默认作息；本项在确认和恢复时显式归一为 `pku`，修复“从其他学校导回北大后仍停留在原学校”的反向路径。
- 其他学校学期的课程不会被删除。恢复点继续绑定被替换的目标 `semesterId`；恢复后也切换回该目标课表，使恢复结果立即可见。
- 当前模型仍以 `users.scheduleId` 同时表示当前课表学校以及导入／新建群组的默认学校；本项通过预览提示明确告知切换效果，没有新增独立的多学校快速切换字段。用户仍可在设置中切换回旧学校查看已保留的数据。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 跨校导入数据层回归 | 新增 3 项真实临时 PostgreSQL 测试：北大→贸大、贸大→北大、目标学期恢复；验证当前课表持久切换、旧学校课程保留及恢复点范围 |
| 相关重点回归 | IMP-02、SCH-01、SCH-02 共 3 个测试文件、10 项通过 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：25 个文件、141 项通过 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |
| 格式检查 | 全工作树 `git diff --check` 通过 |

边界：本项没有执行数据库迁移、部署或生产数据操作；没有开始 TIME-01，也没有加入完整的多学校课表切换器。

### TIME-01：按天视图与服务端计算使用不同时间来源

服务端共同空闲采用北大基础网格并追加部分晚间行，而按天视图通过群组学校作息计算时间标签。非北大群组可能出现“显示为一个时间，实际格子计算的是另一个时间”。

**代码依据：** [服务端显示网格](src/server/availability/data.ts)、[按天视图时间标签](components/common-availability.tsx)。

**建议：** 周视图、日视图、详情、无障碍标签、时长筛选和复制文本统一读取服务端的实际起止时间；跨校场景优先表达钟点，避免暗示各校“第 N 节”相同。

**验收：** 同一格子的所有展示和复制内容一致；切换周／日视图不改变时间含义；追加行有完整时间标签。

#### TIME-01 本次交付记录（2026-09-20）

**结论：本地修复完成，等待本项确认；尚未部署。**

- 服务端展示网格不再固定使用北京大学 12 节作息，而是以群组绑定学期的作息为唯一基准；其他成员晚于群组作息覆盖范围的实际忙碌仍以钟点追加行展示。
- 网格接口为每行返回 `startMin`、`endMin` 和完整时间文本；详情接口返回本次查询使用的同一 `gridRow`，避免前端按节次号重新推算出另一套时间。
- 周总览、按天视图、详情标题、无障碍标签、已过期判断、连续空档、时长／白天晚间筛选和复制文本现在全部使用服务端钟点轴。跨校界面优先显示 `HH:MM–HH:MM`，不再把不同学校的“第 N 节”暗示为同一时间。
- 键盘上下导航改为读取实际网格行数；详情参数允许最多 16 个学校作息行加 4 个晚间追加行，追加行也能通过键盘和详情接口访问。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 非北大时间一致性回归 | 新增真实临时 PostgreSQL 场景：对外经济贸易大学群组第 1 行在网格与详情中均为 `08:00–08:45`，空档轴结束于该校实际末节 `20:50` |
| 相关重点回归 | 时间网格、跨校换算、详情参数、连续空档和时长格式共 5 个测试文件、28 项通过 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：25 个文件、142 项通过 |
| 生产构建 | 显式清空数据库、DirectMail、`AUTH_SECRET`、`AUTH_ORIGIN` 后 `pnpm build` 通过 |
| 格式检查 | 全工作树 `git diff --check` 通过 |

边界：本项没有改变成员各自学校的课程→钟点换算、跨校开学日对齐或隐私投影规则；没有执行数据库迁移、部署或生产数据操作，也没有开始 AI-01。

## 5. 工程与产品完整性

### AI-01：AI 调用额度只保存在进程内存中

当前每日 10 次限制由进程内 `Map` 维护，重启会重置，多实例之间不共享。因此该提示不能作为稳定的全站配额承诺。

**依据：** [AI 导入次数限制](app/api/import/ai/route.ts)。

**建议与验收：** 使用数据库或共享存储进行原子计数；明确每日重置时区以及失败请求是否计数。重启、多实例和并发调用下保持同一账号的限制一致。

#### AI-01 本次交付记录（2026-09-20）

**结论：本地修复完成，等待本项确认；尚未部署。**

- 删除路由进程内的 `Map`，复用 `0007` 已有的 `auth_rate_limits` 表，以账号 ID 命名空间持久化 AI 识别额度；不新增数据库迁移。
- 额度通过单条 PostgreSQL upsert 原子保留，同一数据库下的应用重启和多实例共享计数；并发请求无法共同越过每天 10 次上限。
- 每日边界明确为 `Asia/Shanghai` 00:00。界面和 429 错误都显示“北京时间 00:00 重置”。
- 失败计数规则明确为：未登录、请求／课表内容无效、AI 密钥未配置和本地学期准备失败不计数；额度保留后进入智谱调用，无论供应商超时、限流、网络失败、模型结果无效或后续预览保存失败，都计入一次。
- 额度检查从请求入口后移到所有本地校验和配置检查之后、外部 AI 调用之前，避免用户因无效输入损失次数。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 持久额度与路由计数规则 | AI 配额、AI 路由、AI 提取共 3 个测试文件、16 项通过；覆盖北京时间边界、账号隔离、12 次并发仅放行 10 次、无效输入不计数和供应商失败计数 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元测试 | `pnpm test`：26 个文件、150 项通过 |
| 生产构建 | 显式清空数据库、邮件、认证和智谱密钥环境变量后 `pnpm build` 通过 |
| 格式检查 | 全工作树 `git diff --check` 通过 |

边界：本项没有调用真实智谱接口，没有对生产数据库执行读写、迁移或部署；临时 PostgreSQL 测试证明了 SQL 原子语义与持久表状态，但不能替代真实连接池、多进程和部署平台的压力测试。

### QA-01：测试需要覆盖完整业务链路

已有 54 项测试为良好基础，但没有阻止此次发现的跨接口问题。应优先补充以下有实际业务价值的回归场景：

| 场景 | 必须验证的结果 |
| --- | --- |
| 已验证邮箱重新登录 | 没有密码或有效验证凭证时不能建立会话 |
| 老账号首次设密码 | 必须重新验证邮箱，保留原用户及数据关联 |
| 找回密码 | 凭证短时有效、只能使用一次，重置后旧会话失效 |
| 单周／批量跳过 | 只影响对应周；个人课表与群组结果一致 |
| 两用户自定义作息 | 数据隔离，取消预览无正式副作用 |
| AI 切换学校 | 采用页面确认的作息上下文 |
| 跨学校导入 | 成功后可见，目标学期正确，恢复范围正确 |
| 跨校查看 | 时间标签、详情、复制文本和日期换算一致 |
| 午间及课间忙碌 | 连续空档不会跨过忙碌区间 |
| 未录入与确认无课 | 状态和可用人数正确区分 |
| 学期超过 16 周 | 正常录入、预览、查询与展示 |
| 手机及键盘交互 | 焦点、面板关闭、错误恢复与主要操作可用 |

使用隔离测试环境和明确标注的测试账号，不用真实用户数据做写入测试。

#### QA-01 本次交付记录（2026-09-20）

**结论：关键业务回归矩阵已建立并全部通过，用户已确认；尚未部署。**

本项先审计现有测试，再补缺口，没有为已经充分覆盖的行为重复编写表面断言。原清单的自动化证据如下：

| 关键场景 | 自动化证据与当前保证 |
| --- | --- |
| 已验证邮箱重新登录 | `login-routes.test.ts` 经过真实临时 PostgreSQL、密码校验、会话 Cookie 和路由边界，验证未知邮箱、错误密码、无密码老账号不能建立会话 |
| 老账号首次设密码 | 同一测试验证必须重新完成邮箱 challenge，原用户 ID、课程、群组成员关系和隐私设置保持不变 |
| 找回密码 | 同一测试验证凭证绑定邮箱／用途／challenge ID、15 分钟有效、仅消费一次；重置后旧密码和全部旧会话失效，事务失败完整回滚 |
| 单周／批量不去 | `group-availability.test.ts` 现同时覆盖单周跳过与批量替换；个人课表 `skippedWeeks` 和跨校群组对应教学周的空闲结果一致，越权及超出课程周次被拒绝 |
| 两用户自定义作息 | `custom-schedule.test.ts` 经过迁移和真实 SQL，验证用户隔离、预览无正式副作用、确认后才写入目标用户作息 |
| AI 切换学校 | `ai-route.test.ts` 验证文字、图片和自定义网格均采用页面所选作息，保存到预览的上下文一致 |
| 跨学校导入 | `target-schedule.test.ts` 验证目标学校持久切换、旧学校课程保留、恢复只影响被替换学期 |
| 跨校查看 | `ranges.test.ts` 与 `group-availability.test.ts` 验证成员按各自开学日和作息换算，网格、详情、空档和复制所依赖的钟点轴一致 |
| 午间及课间忙碌 | `free-intervals.test.ts` 与群组集成测试验证空档不会跨越网格行间或相邻节次间的实际忙碌 |
| 未录入与确认无课 | 群组集成测试验证未知、确认无课、仅有忙碌三种状态对人数、详情和共同空闲结论的不同影响 |
| 学期超过 16 周 | `semester-weeks.test.ts`、导入器测试及新增 `import-preview-editor.test.tsx` 覆盖第 17–20 周录入、预览、查询、群组计算、失败重试和最终提交 |
| 手机及键盘交互 | 新增四个 jsdom 组件测试文件：窄屏视口下可切换按天视图和使用移动导航；课表格支持方向键；详情弹窗支持焦点进入、Tab 循环、Escape 关闭及焦点归还；主题下拉可纯键盘搜索选择；接口失败后可见重试并恢复主流程 |

测试基础设施与新增回归：

- Vitest 现在同时收集 `src/**/*.test.ts` 和 `components/**/*.test.tsx`；加入 Testing Library、user-event 和 jsdom，仅作为开发依赖。
- 组件测试渲染真实 React 组件并模拟 fetch／Next 导航，不依赖生产账号或数据库；服务端集成测试继续在进程内临时 PostgreSQL 引擎执行全部迁移。
- 共同空闲交互测试覆盖窄屏主流程、方向键二维移动、详情获取、Escape 关闭、焦点归还和请求失败后的“重试”。
- 导入预览交互测试覆盖 20 周学期、首次确认失败后的错误恢复、再次提交以及完整周次数组不丢失。
- 批量不去测试覆盖个人课表与群组结果的同源性，并明确处理跨校开学日相差一周的映射。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 新增重点回归 | 4 个组件测试文件的 5 项交互测试，加 1 项真实 SQL 批量不去业务链路测试，全部通过 |
| TypeScript | `pnpm exec tsc --noEmit --incremental false` 通过（退出码 0） |
| 全部单元与集成测试 | `pnpm test`：30 个文件、156 项通过 |
| 生产构建 | 显式清空数据库、邮件、认证和智谱密钥环境变量后 `pnpm build` 通过 |
| 格式检查 | 全工作树 `git diff --check` 通过 |

边界：jsdom 验证 DOM 语义、状态与键盘／焦点行为，不执行真实 CSS 布局、触控手势或屏幕阅读器；手机视觉几何、软键盘遮挡、Safari／微信内置浏览器差异以及真实多连接 PostgreSQL 并发仍须在预发布和真机验收。测试只使用内存数据库、模拟接口和明显标注的测试身份，没有读取或写入生产数据。

### GROUP-01：群组生命周期管理

目前已具备创建、加入、隐私设置及邀请码更新。建议后续补充退出、改名、归档和群主管理，并定义群主退出时的转让或关闭规则。

这属于产品规划，不是对现有群组执行变更的授权。实现时需要清晰的权限规则、影响提示和必要的恢复机制，不能将归档等同于清空成员或课程。

#### GROUP-01 本次交付记录（2026-09-20）

**结论：群组生命周期已形成可恢复、权限明确的闭环，等待本项确认；尚未部署，也未对任何生产群组执行操作。**

最终规则如下：

- 普通成员可以主动退出活跃或已归档群组；退出只移除本人在该群的成员关系与群内隐私覆盖，不删除账号、个人课程或其他群组关系。
- 群主可以修改群名、移出普通成员、把群主身份转让给群内成员、归档和恢复群组。移出成员同样不删除其账号或课表。
- 群主不能直接退出。多人群先转让群主再退出；只剩群主时使用归档。服务端和界面都执行这一规则。
- 归档只写入 `groups.archived_at`，保留群组行、成员关系、隐私设置、课程与历史数据。归档后不出现在共同空闲的普通群组列表，邀请码、隐私修改、改名、成员管理和共同空闲接口暂停；群主恢复后原关系和邀请码重新生效。普通成员仍可在归档状态退出。
- 群主转让在一个数据库事务中更新 `groups.owner_id` 和全部成员角色，保证最终只有一条 `OWNER` 角色。自定义作息群只有在新旧群主同学期作息完全一致时才能转让，避免群组时间轴和共同空闲结果被静默改变。
- 加入、归档、隐私更新、移出和转让会锁定对应群组行，避免归档与加入／管理请求并发时越过状态检查。

界面与接口：

- 群组页分为“使用中的群组”和“已归档”，群主可展开管理区改名、转让、移出成员和归档；归档卡片明确说明数据仍保留并提供恢复入口；普通成员可退出。
- 归档、退出、移出和转让均在操作前展示具体影响。文案明确账号、课表、成员关系是否保留。
- 新增 `PATCH/DELETE /api/groups/[groupId]` 与 `PATCH/DELETE /api/groups/[groupId]/members/[userId]`；原群组列表默认排除归档项，群组管理页显式请求归档项。只读管理页增加群组状态，并把归档邀请码标为停用。
- 新增纯增量迁移 `0010_group_lifecycle.sql`，只增加可空时间列；既有行默认未归档，不重写或删除任何数据。生产执行前仍需按数据安全守则展示 SQL、备份并取得明确授权。

自动化验证：

| 验证 | 结果 |
| --- | --- |
| 生命周期真实 SQL | 新增 6 项 PGlite 集成测试：迁移保留旧数据；归档／恢复；邀请码和共同空闲停用；退出；移出；转让；自定义作息转让保护 |
| 页面操作 | 新增 2 项 jsdom 交互测试：群主改名、归档、保留提示、恢复；归档群成员退出及影响确认 |
| 数据保留 | 退出与移出后账号和个人课程仍存在；归档／恢复后成员与群内隐私覆盖保持不变 |
| 全部单元与集成测试 | `pnpm test`：32 个文件、164 项通过 |
| TypeScript 与格式 | `pnpm exec tsc --noEmit --incremental false`、`git diff --check` 均通过 |
| 生产构建 | `pnpm build` 通过；新动态路由均被 Next.js 正确收集 |

边界：迁移文件尚未用于生产数据库；浏览器交互由 jsdom 覆盖语义和请求，没有用真实账号或真实群组做写入验收。归档属于可恢复状态，不提供永久删除群组入口。多实例并发依赖 PostgreSQL 行锁，真实连接池压力仍应在预发布环境验证。

### DOC-01：文档与实现同步

README、实施说明及部署说明应同步多校作息、AI 导入、恢复点、认证方式、环境依赖和已知限制。尤其不能保留会让维护者误认为所有功能已验收、或邮箱验证仍只是未来计划的描述。

建议密码方案落地后统一更新登录说明和用户可见文案，确保文档、界面和实际行为一致。

#### DOC-01 本次交付记录（2026-09-20）

**结论：现行文档已经与本轮代码实现统一，等待本项确认；这次只修改仓库文档和示例环境变量，没有部署或操作生产数据库。**

文档职责与事实来源已经明确：

- `README.md` 作为项目入口，说明当前已实现能力、本地启动方式、验证范围、文档导航和已知限制，并明确区分“本地通过”与“已经上线”。
- `IMPLEMENTATION.md` 作为当前实现合约，记录密码认证、会话边界、32 套学校／校区／季节预设、用户级自定义作息、动态教学周、Excel／AI 导入、7 天恢复点、隐私投影、群组生命周期和实际 API 方法。
- `DEPLOY.md` 作为部署与运维手册，统一环境变量、Vercel 预览、国内服务器 HTTPS、`0007`–`0010` 迁移影响、生产备份与授权要求、限额、学期配置、导入恢复和上线验收。
- `.env.example` 补齐 DirectMail Region 与智谱密钥，认证密钥说明改为注册、老账号首次设密码和找回密码使用。
- 旧版 `课隙_OpenPeriod_SPEC_UI_V1.md` 保留历史正文，在顶部增加醒目标记，明确“邮箱可直接恢复身份”“仅 PKU”“固定 16 周”“多学校属于未来功能”等说法已经失效，并链接到现行文档。

重点偏差已经消除：

| 原文风险 | 当前文档事实 |
| --- | --- |
| 将产品概括为“邮箱身份 Session＋PKU Excel” | 日常使用邮箱＋密码，验证码只服务注册／过渡／找回；导入包含多校 Excel 和可选 AI |
| 把已验证邮箱直接建会话写成现行流程 | 明确只有密码校验或带用途的有效邮箱 challenge 可以建立新会话 |
| 把多校作息、邮箱验证和自定义作息列为未来功能 | 记录现有预设、用户级自定义作息、动态教学周及跨校钟点换算 |
| 暗示代码已部署或生产迁移已完成 | README 与 DEPLOY 均明确本轮只在本地完成，备案、HTTPS、部署和生产迁移仍待执行 |
| 忽略恢复点和群组归档的数据边界 | 明确恢复点只覆盖课程、时段和“不去”，不含私人忙碌；归档保留群组、成员和隐私关系 |

核对与验证：

| 检查 | 结果 |
| --- | --- |
| 应用环境变量与 `.env.example` | 当前代码使用的项目变量均已列出；`NODE_ENV` 为运行平台内建变量，不要求用户填写 |
| Markdown 相对链接 | README、实施基线、部署指南、历史 SPEC 的仓库内链接全部存在，0 个失效链接 |
| API 方法 | 按 `app/api/**/route.ts` 枚举核对；包括 `PUT /api/schedule/confirm-empty` 与反馈 GET／POST |
| 旧断言扫描 | 现行文档不再把免密邮箱登录、仅 PKU、固定 16 周或“多校未来实现”当作当前行为；历史 SPEC 顶部已标明失效范围 |
| 自动化与构建 | `pnpm test`：32 个文件、164 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：历史 V1 SPEC 的正文为保留设计档案，没有逐段改写；现行行为以 README、实施基线和部署指南为准。第三方邮件／AI 的真实送达和服务条款、正式域名 HTTPS、真机 UI 以及生产迁移仍需在对应环境验收，本文没有把这些未执行事项记为已完成。

## 6. 前端审美与体验问题

| 编号 | 当前问题 | 修改建议及验收关注 |
| --- | --- | --- |
| UI-01 | 文楷覆盖正文、表单和数字，小字号及密集课表不够清晰 | 文楷用于品牌和少量标题；正文、控件、时间使用无衬线；验证中英文混排、长邮箱、时间数字和手机小屏 |
| UI-02 | 奶油底、点阵、卡片阴影与彩色玻璃热力格叠加，装饰较多 | 保留温暖品牌，减少数据区纹理、重复卡片和阴影；让课程与可约时间成为视觉重点 |
| UI-03 | 热力图高空闲比例切白字，对比度偏低 | 保留连续渐变，改用深色数字或降低背景明度；人数、符号与颜色共同表达状态 |
| UI-04 | 群组、成员、时间筛选与结果的层级可以更清楚 | 围绕“选谁—找到空档—复制时间”安排页面，提升候选空档可见性，辅助信息降低权重 |
| UI-05 | 共同空闲手机默认周视图；我的课表仍依赖宽网格横向滚动 | 手机优先按天／今天，保留周总览切换；两页采用一致的日期导航和详情交互 |
| UI-06 | 控件、字符图标、圆角、字号与状态表现缺少完整统一规范 | 统一设计变量及组件，采用一致 SVG 图标；验证焦点、选中、禁用、错误和成功状态 |
| UI-07 | 动效分散，加载和筛选更新缺少整体节奏 | 建立统一动效规则；更新时维持布局稳定，避免大量格子独立闪动；完善减少动态效果模式 |

### UI-01 本次交付记录（2026-09-20）

**结论：字体职责、字号层级和数字排版已完成第一阶段统一，用户已确认；没有改动页面结构、业务文案、颜色或功能。**

- 全站正文、表单、按钮和用户数据改用系统无衬线栈，优先使用 macOS／iOS 的系统字体、苹方，Windows 的 Segoe UI／微软雅黑及 Linux 常见的 Noto／思源黑体。文楷只用于“课隙”品牌字和一级页面标题。
- 增加 `--font-sans`、`--font-brand`、`--font-mono` 三类字体变量；控件和 `textarea` 明确继承正文，管理页技术标识使用等宽字体。
- 字重收敛到 400／500／600／700，移除依赖浏览器合成的 450／550／650／750 中间值。
- 教学周、节次、时间、空档列表、邀请码、导入统计和管理统计统一使用 `tabular-nums`，同一列中的数字宽度稳定。
- 共同空闲节次时间从 9.5px 提升到 11px；“我的课表”课程名从 11px 提升到 12px，课程辅助信息从 9px 提升到 10.5px，左侧节次时间从 9px 提升到 10.5px，“本周”与“不去”标记也不再低于 10px。
- 字体入口从完整 `lxgw.css` 切换为 `brand.css`。声明由 208 KB、194 个常规／粗体分片减少到 11.4 KB、7 个固定品牌标题所需的粗体分片，CSS 声明体积减少约 94.6%；旧完整字体源保留，方便以后审阅后扩展品牌字符。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 真实浏览器桌面 | 1440×1000 欢迎页和代表性课表通过视觉检查；正文为系统无衬线，品牌和一级标题为文楷，课表文字未碰撞或溢出 |
| 真实浏览器手机 | 390×844 欢迎页和代表性课表通过视觉检查；页面无整体横向溢出，宽课表仍只在既有滚动容器内横向浏览 |
| 计算样式 | 正文、标题和品牌字体职责正确；课程名 12px、辅助信息与节次时间 10.5px，数字启用等宽排版 |
| 字体请求 | 欢迎页请求精简后的 `brand.css` 和 6 个实际命中的粗体分片，字体文件约 282 KiB；正文不再触发文楷常规字重分片 |
| 自动化与构建 | `pnpm test`：32 个文件、164 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器检查使用公开欢迎页和与现有组件类名一致的代表性课表数据，没有登录或读取真实账号。不同操作系统会选用各自系统无衬线字体，正式上线前仍应在 Windows、Android、iOS 和微信内置浏览器各做一次字体回退实看。

证据入口：[全局样式](app/globals.css)、[共同空闲组件](components/common-availability.tsx)、[我的课表](components/my-schedule.tsx)、[登录表单](components/identity-form.tsx)、[公共布局](components/app-shell.tsx)。

### UI-02 本次交付记录（2026-09-20）

**结论：数据页面的装饰层级已收敛，保留温暖品牌和状态颜色，用户已确认；没有改动页面结构、业务文案或功能。**

- 移除全站奶油底上的重复点阵，保留单一暖白画布，避免页面背景与课表数据同时争夺注意力。
- 普通数据容器改为边框分区：共同空闲工作区、群组卡片、导入预览、个人课表、设置卡片和空状态不再使用常驻阴影。登录／引导表单继续保留轻阴影，详情弹窗、编辑器和反馈浮层使用更明确的浮层阴影。
- 共同空闲热力格从渐变、半透明背景、背景模糊和内高光的叠加效果，改为单层语义色。空闲人数对应的色相、饱和度和明度映射保持不变，本轮不改变业务含义。
- 课表网格间隔由彩色渐变改为 1px 中性边线；悬停反馈只做轻微亮度变化，不再额外增强饱和度。
- 群组管理区取消内嵌卡片底色与圆角，改用顶部分隔；学期信息和管理统计取消小卡片堆叠，改用短竖线与留白组织内容。
- 页面内主操作、次操作和危险操作仍通过颜色、边框及文字区分；去除不参与层级表达的按钮阴影。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 真实浏览器桌面 | 1440×1000 欢迎页和代表性共同空闲页通过视觉检查；入口卡片仍有清晰层级，数据页无点阵和常驻卡片阴影，热力格仍能按人数显示连续颜色 |
| 真实浏览器手机 | 390×844 代表性共同空闲页通过视觉检查；页面无整体横向溢出，宽课表只在原有网格容器中横向滚动，筛选、成员与候选空档顺序保持不变 |
| 计算样式 | 页面背景图、数据工作区阴影、热力格渐变／背景模糊和课表彩色分隔均已移除；欢迎页表单阴影与弹窗浮层阴影保留 |
| 自动化与构建 | `pnpm test`：32 个文件、164 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器检查使用公开欢迎页和与现有组件类名一致的代表性课表数据，没有登录、读取真实账号或操作生产数据。热力格文字颜色和无障碍对比度属于 UI-03，本项只减少表面效果并保留原有语义色映射。

证据入口：[全局样式](app/globals.css)、[共同空闲热力格](components/common-availability.tsx)、[群组页](components/groups-dashboard.tsx)、[设置页](components/settings-panel.tsx)、[个人课表](components/my-schedule.tsx)。

### UI-03 本次交付记录（2026-09-20）

**结论：热力格文字对比度和非颜色状态表达已完成校正，用户已确认；没有改动空闲计算、成员筛选、详情语义或页面结构。**

- 所有比例统一使用 `--text-primary` 深色文字，取消高空闲比例切换白字。红、黄、绿各段的数字都以同一前景色显示，视觉层级更稳定。
- 每个非加载格都直接显示“有空人数／所选人数”；0 人格从短横线改为 `0/人数`，全员格显示 `人数/人数 ✓`，部分空闲格继续显示比例。用户无需仅凭红绿色判断状态。
- 屏幕阅读器仍读取包含日期、时间、是否过去和成员状态的完整 `aria-label`；格内紧凑数字标记继续对辅助技术隐藏，避免重复朗读。
- 图例说明改为“颜色表示空闲比例，格内显示人数”，明确颜色与数字的分工；原有连续色相映射和点击详情行为保持不变。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 浏览器计算对比度 | 在实际 CSS 合成背景上抽样 `0/4`、`1/4`、`2/4`、`3/4`、`4/4`，最低为 8.55∶1，高于普通文字 4.5∶1 的目标；原高比例白字约为 1.7–1.8∶1 |
| 真实浏览器桌面 | 1440×1000 代表性共同空闲页通过视觉检查；0、部分和全员状态均可直接读出，数字与勾号没有碰撞 |
| 真实浏览器手机 | 390×844 通过视觉检查；页面无整体横向溢出，11px 格内数字清晰，课表仍只在原有容器内横向滚动 |
| 自动化与构建 | `pnpm test`：32 个文件、164 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器检查使用与现有组件类名和配色算法一致的代表性四人课表数据，没有登录、读取真实账号或操作生产数据。对比度按浏览器计算色与课表底层颜色合成后计算；正式上线前仍应在目标设备和显示器上做一次实看。

证据入口：[热力格渲染与语义](components/common-availability.tsx)、[热力格样式](app/globals.css)。

### UI-04 本次交付记录（2026-09-20）

**结论：共同空闲页已围绕“选择成员—找到空档—复制时间—检查整周”重排信息层级，用户已确认；没有改动空闲计算、筛选规则、复制内容或详情数据。**

- 群组和教学周保留为页面上下文，成员选择紧随其后；结果加载完成后，“推荐空档”现在先于整周矩阵出现，用户无需穿过整张课表才能找到可行动结果。
- 原“连续空档”改为“推荐空档”，增加一句简短用途说明和结果数量；星期、最短时长、白天／晚间筛选保持原功能，并和推荐结果放在同一视觉区。
- 每条候选结果重排为日期与来源说明、具体钟点与时长、`复制时间` 操作三段。复制内容及成功反馈保持原逻辑，按钮名称更明确。
- “周总览／按天”从群组和教学周控制区移动到“整周分布”标题旁，使它只控制的矩阵视图与控件建立直接关系。
- 整周矩阵增加独立标题和辅助说明，图例文案缩短；矩阵作为比较和检查工具保留全部格子、键盘移动、详情打开及内部横向滚动。
- 手机端推荐结果采用两列排版，复制按钮与日期、时间稳定对齐；整周标题、查看方式和图例在下方形成独立区段。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 信息顺序 | 浏览器 DOM 与视觉位置均确认推荐空档位于整周矩阵之前；成员选择、筛选、复制和矩阵形成连续阅读顺序 |
| 真实浏览器桌面 | 1440×1000 代表性四人结果通过视觉检查；3 条候选、筛选控件与复制操作在首屏内可见，矩阵标题和查看方式关系清楚 |
| 真实浏览器手机 | 390×844 通过推荐结果和矩阵两个滚动位置的视觉检查；复制按钮最小高度 40px，页面无整体横向溢出，课表仍只在原容器内横向滚动 |
| 自动化与构建 | `pnpm test`：32 个文件、164 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器视觉检查使用与现有组件类名一致的代表性四人数据，没有登录、读取真实账号或操作生产数据。候选数量较多时页面会自然增长，用户可用既有星期、时长和时段筛选缩小结果；本项没有增加折叠、分页或排序规则。

证据入口：[共同空闲页面结构](components/common-availability.tsx)、[共同空闲页面层级样式](app/globals.css)。

### UI-05 本次交付记录（2026-09-20）

**结论：共同空闲与我的课表已统一为手机按天优先、桌面周总览优先，并保留双向切换；用户已确认。**

- 共同空闲页在不改变服务端渲染初始标记的前提下，手机宽度首次挂载后自动进入既有按天视图；桌面仍默认周总览，用户在两端均可随时切换。
- 我的课表增加与共同空闲一致的“周总览／按天”控件和七天日期导航。当前教学周优先选中学期时区下的今天；查看其他教学周时默认从周一开始，用户选择的星期在切周时保留。
- 我的课表按天视图采用纵向时间轴，左列显示实际起止时间，右列显示课程和私人忙碌。跨多节事件按对应时间行连续铺开；课程地点、忙碌重复类型和“本周不去”状态沿用原数据。
- 单日空白时段点击后继续打开原有“标记忙碌”编辑器，课程与忙碌块继续打开原详情／编辑器；周总览的空白格、课程、忙碌和横向滚动交互全部保留。
- 周总览表头补充该教学周的 `月/日`，与按天日期导航一致；个人课表接口只增加学期时区元数据，用于正确选择今天，没有改变课程、忙碌或教学周业务数据。
- 没有课表内容时继续使用原空状态和“确认无课”流程，不展示无意义的视图切换。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 组件交互回归 | 新增我的课表 jsdom 测试，覆盖手机默认按天、星期切换、课程详情、空白时段标记忙碌和切回周总览；共同空闲测试同步覆盖手机默认按天并保留周视图键盘交互 |
| 真实浏览器手机 | 390×844 代表性个人课表通过视觉检查；页面宽度与视口同为 390px，无整体横向溢出；星期按钮 56px、视图按钮 44px；两节课程与忙碌块完整覆盖对应时间行 |
| 真实浏览器桌面 | 1280×900 周总览通过视觉检查；七天日期完整显示，课表容器与页面均无意外横向溢出 |
| 自动化与构建 | `pnpm test`：33 个文件、165 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器几何检查使用与现有组件类名一致的代表性课程和忙碌数据；实际 React 状态与编辑器交互由 jsdom 回归覆盖，没有登录、读取真实账号或操作生产数据。本项未加入左右滑动切日手势，星期切换使用清晰的七日按钮；微信内置浏览器及 iOS／Android 真机仍应在预发布环境补充验收。

证据入口：[共同空闲响应式视图](components/common-availability.tsx)、[我的课表响应式视图](components/my-schedule.tsx)、[个人课表时区数据](src/server/schedule/data.ts)、[全局响应式样式](app/globals.css)、[共同空闲组件测试](components/common-availability.test.tsx)、[我的课表组件测试](components/my-schedule.test.tsx)。

### UI-06 本次交付记录（2026-09-20）

**结论：控件形状、图标体系、焦点与语义状态已完成统一；用户已确认。**

- 增加控件、面板、浮层和胶囊四级圆角变量，以及统一焦点环、禁用透明度、警告和危险状态变量。普通按钮和表单控件使用 8–10px 圆角，内容容器使用 14–16px，筛选标签和徽标保留胶囊形；主操作不再因颜色不同而随意变成胶囊。
- 引入单一的 Phosphor SVG 图标体系。侧边栏和底部导航、返回、下拉、周切换、添加、删除、关闭、上传、复制成功与状态说明中的字符图标已替换；服务端页面使用该图标库的 SSR 入口，避免为静态返回图标增加客户端边界。
- 图标统一继承文字颜色和当前状态，选中导航继续使用陶红强调色；仅表达装饰或重复文字含义的 SVG 对辅助技术隐藏，原按钮文字和 `aria-label` 保留，图标替换没有削弱可访问名称。
- 表单输入、下拉和按钮统一使用 2px 品牌色焦点环及 2px 外偏移；禁用状态统一降低透明度并禁止点击。错误、成功、警告和危险操作分别使用语义色变量，群组危险区、导入警告／错误、冲突提示和退出按钮不再散落重复色值。
- 导入核对页的课程时段行增加独立删除列；手机端删除操作固定在相应网格位置，避免加入 SVG 后挤压周次输入。导航、表单、课表和导入操作的图标尺寸与文字间距采用同一套规则。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 字符图标审计 | `app` 与 `components` 的可见界面代码中未残留原导航、勾选、关闭、加号、上传、方向或心形字符图标；命中的箭头仅存在于两处代码注释 |
| 组件回归 | 下拉选择、共同空闲、我的课表、群组、导入核对和对话框行为共 6 个文件、8 项测试通过 |
| 真实浏览器桌面 | 1280×900 课表外壳和加入群组表单通过视觉检查；侧边栏 4 个导航 SVG，主按钮与输入框均为 10px 圆角；聚焦输入框为 2px 品牌色焦点环并有 2px 外偏移 |
| 真实浏览器手机 | 390×844 课表外壳通过视觉检查；底部导航 4 个 SVG，四个触控区域均为 67px 高，页面宽度与视口同为 390px，无整体横向溢出 |
| 全量验证 | `pnpm test`：33 个文件、165 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器检查使用本地开发环境的公开页面和无真实账号的应用外壳；控制台只出现开发服务器 HMR WebSocket 握手错误，生产构建通过。本项没有登录、读取或修改真实用户数据，也没有改变业务接口、课程数据或生产配置。加载和筛选更新的动效节奏、骨架屏及减少动态效果模式属于 UI-07。

证据入口：[设计变量与组件状态](app/globals.css)、[应用导航](components/app-shell.tsx)、[统一下拉](components/themed-select.tsx)、[共同空闲](components/common-availability.tsx)、[我的课表](components/my-schedule.tsx)、[导入核对](components/import-preview-editor.tsx)、[图标依赖](package.json)。

### UI-07 本次交付记录（2026-09-20）

**结论：动效时长、浮层进入、加载反馈、数据更新和减少动态效果模式已完成统一；本地实现与验证完成，等待本项确认。**

- 建立三档动效变量：120ms 用于控件与下拉，180ms 用于状态和桌面浮层，240ms 用于手机底部面板；统一使用标准和强调两条缓动曲线。导航、筛选、按钮、拖放区、下拉箭头、提示条和浮层不再各自声明近似但不同的时长。
- 浮层背景统一淡入，桌面对话框使用 8px 短距离进入，手机底部面板使用 28px 短距离进入；主题下拉增加 120ms 的轻微淡入。没有给整页、课表格或列表逐项添加级联动画。
- 新增共用 `LoadingState`：展示明确的加载文案、静态骨架和一条进度轨迹。共同空闲、群组、个人课表和导入核对使用同一反馈，管理页加载状态也补齐 `role="status"`、`aria-live` 与 `aria-busy`。
- 共同空闲切换教学周或成员时保留已有推荐结果和矩阵尺寸，覆盖“正在更新共同空闲”状态并暂时锁定旧结果，避免整张矩阵的每个格子独立闪烁。请求序号保证较早响应不能覆盖最新选择；打开时段详情时对应格子也会暴露忙碌状态。
- 个人课表首次进入使用统一骨架；切周或保存后刷新时保留现有课表高度，显示“正在更新第 N 周课表”并暂时锁定旧内容。查看信息、添加课程和标记忙碌在初始数据到达前不可误触。
- `prefers-reduced-motion: reduce` 下关闭加载循环、将动画与过渡缩短为即时、关闭平滑滚动，并保留静态进度位置和全部状态文案。导入警告的定位滚动也会读取系统偏好，不再强制平滑移动。

验证结果：

| 验证 | 结果 |
| --- | --- |
| 更新稳定性回归 | 新增共同空闲测试，确认第二个教学周请求等待期间原 14 个结果格仍在、结果区标记 `aria-busy=true`，响应后状态正确解除 |
| 初始加载回归 | 新增个人课表测试，确认接口响应前显示可读加载状态和 `aria-busy=true`，数据到达后替换为可交互日视图 |
| 标准动效浏览器计算 | 下拉 120ms、桌面浮层 180ms、手机底部面板 240ms；加载轨迹为唯一循环动画，结果更新状态不接收指针事件 |
| 减少动态效果浏览器计算 | 媒体查询生效；浮层、下拉和控件过渡为即时，加载轨迹 `animation-name: none`，HTML 滚动行为为 `auto` |
| 响应式视觉检查 | 1280×900 加载／更新状态和浮层通过视觉检查；390×844 底部面板宽度与视口同为 390px，无横向溢出 |
| 全量验证 | `pnpm test`：33 个文件、167 项通过；TypeScript、生产构建与 `git diff --check` 通过 |

边界：浏览器检查从公开加入群组页加载实际样式，再注入与生产类名一致的隔离状态样例，没有登录或读取真实账号。开发控制台仅出现既有 HMR WebSocket 握手错误，生产构建通过。本项没有改变课表计算、接口数据、数据库或生产配置。

证据入口：[动效与加载规范](app/globals.css)、[共用加载状态](components/loading-state.tsx)、[共同空闲更新流程](components/common-availability.tsx)、[个人课表更新流程](components/my-schedule.tsx)、[导入定位与加载](components/import-preview-editor.tsx)、[共同空闲回归测试](components/common-availability.test.tsx)、[个人课表回归测试](components/my-schedule.test.tsx)。

字体资源目录约 9.4 MB，但当前按字符范围分片，不能将目录总大小直接当作单页下载量。字体优化应先测实际加载资源，再决定是否缩减字重和使用范围。

## 7. 三套 UI 改版方案

以下均为待选择方案，不代表已批准实施。共同前提是保留核心功能，并修复影响展示可信度的数据问题。

| 方案 | 视觉和字体 | 信息结构 | 动效 | 范围 |
| --- | --- | --- | --- | --- |
| A：温润精修（推荐） | 奶油白、陶红、墨绿；文楷仅用于品牌与少量标题，正文无衬线；减少纹理和阴影 | 保留现有导航；桌面“课表＋候选空档”，手机日视图优先 | 轻淡入、短距离面板移动、清楚的操作反馈 | 中等 |
| B：清晰的日历工具 | 白色与浅灰底、深灰文字、单一强调色；统一无衬线和等宽数字 | 窄导航、大日历、固定右侧详情，方便连续比较 | 快速、克制，主要使用颜色与透明度变化 | 中到较大 |
| C：轻松的校园约时间 | 暖白配柔和青绿，圆润清晰的无衬线；突出头像和成员状态 | 首页先显示最近可约时间，周矩阵作为展开视图；日期和时长筛选更突出 | 卡片与成员选择稍活泼，数据区域保持稳定 | 较大 |

推荐 A 的视觉方向，结合 B 的日历排版和固定详情交互。保留课隙的校园亲切感，同时提高每天查看和比较时间的效率。

### 建议的统一规范

- **字体：** 正文优先使用 PingFang SC、Microsoft YaHei 等系统无衬线；品牌和少量标题保留文楷。桌面正文 14–15px、手机正文 15–16px。关键时间不缩到约 10px，数字使用 `tabular-nums`。
- **字重：** 采用少量明确层级，确保实际加载的字重与设计匹配，避免大量中间字重依赖浏览器合成。
- **颜色：** 陶红用于主操作和选中态，绿色表达可用程度，警告与错误独立使用语义颜色；未知状态有单独表示。
- **热力图：** 保留已有连续渐变方向，同时显示人数或明确状态；不能只依赖红绿差异。
- **组件：** 统一按钮、输入框、下拉框、圆角、间距和焦点样式；减少卡片套卡片，明显阴影主要用于浮层。
- **触控：** 主要按钮建议约 44px 高，作为本项目体验目标；不要把它误写成所有 WCAG AA 控件统一要求的最低尺寸。
- **首页：** 集中安排群组、日期和成员筛选；候选空档可直接查看成员及复制时间；说明文案从结果区域适度后移。
- **导入：** “学校和学期 → 来源 → 核对 → 确认”；核对页优先显示不确定字段、冲突和替换范围，并保留恢复说明。
- **手机：** 日期导航一致、按天优先、详情采用底部面板；反馈入口不遮挡主操作和底部导航。

### 动画与状态规则

| 场景 | 建议 |
| --- | --- |
| 按钮悬停、成员选择 | 120–160ms 背景／边框变化，控件尺寸保持不变 |
| 日期和周切换 | 保留布局；120–180ms 更新内容，明确显示加载中，避免把旧结果误认为新结果 |
| 详情面板 | 桌面右侧、手机底部，约 200–240ms；关闭更短；支持 Escape 和焦点恢复 |
| 保存、复制成功 | 就地显示勾选与文字，约 1.5–2 秒后恢复 |
| 加载 | 骨架与最终结构一致，不让整片格子持续强烈闪动 |
| 错误 | 错误信息靠近发生位置，保留输入和重试入口 |
| 减少动态效果 | 保留必要状态反馈，关闭位移、缩放和循环闪动；现有适配继续完善 |

避免为每个单元格叠加不必要的模糊和动画。是否存在实际渲染性能瓶颈，应以目标设备测量为准，本轮未进行性能剖析。

## 8. 已有改善，应保留并回归验证

- 主页面已使用统一导航和公共布局。
- 共同空闲已有日期、教学周切换和按天查看。
- 导入前已有恢复快照机制。
- 编辑课程时，未变化的时段会保留其跳过记录。
- 弹窗已有焦点恢复逻辑，样式已有减少动态效果适配。
- 热力图已有连续空闲比例渐变，应在此基础上改善可读性。

这些属于当前代码已有能力，不应再次作为“完全缺失功能”重做；后续应验证其与新认证、多校及新布局的兼容性。

## 9. 建议实施顺序与数据边界

1. **认证与入口安全：** 完成密码方案设计、注册／找回／老用户过渡、HTTPS 与会话切换计划，关闭仅凭邮箱建立会话的路径。
2. **数据隔离与计算准确性：** 修复跳过周次、自定义作息隔离、真实钟点空档计算、未知状态和跨校完整度。
3. **导入与校历一致性：** 统一导入上下文、目标课表、各视图时间来源和教学周数；补充跨流程测试。
4. **基础视觉规范：** 选定 UI 方向，统一字体、色彩、控件和状态，先解决小字及热力图可读性。
5. **布局与动效：** 调整桌面候选空档、手机日视图、详情面板及统一动画，完成响应式和键盘验收。
6. **产品与维护：** 完善持久化限额、群组生命周期、文档和长期维护规则。

所有生产迁移与写操作继续遵守 [AGENTS.md](AGENTS.md)：不得未经明确具体授权删除真实账号或变更群组数据；生产写入先备份或在事务中处理，并向项目所有者展示将执行的 SQL。测试只使用明确标注的自建临时测试账号。

尤其要区分“修复代码”和“迁移历史数据”：本文件并未授权清理账号、改变群组成员、批量重写课表或撤销线上会话。后续应先形成具体、可审阅的迁移范围和回退方案。

## 10. 参考依据

- [OWASP：密码存储](https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html)
- [OWASP：忘记密码与恢复流程](https://cheatsheetseries.owasp.org/cheatsheets/Forgot_Password_Cheat_Sheet.html)
- [W3C：文字对比度](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)
- [W3C：目标尺寸](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html)
- [MDN：减少动态效果偏好](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)
